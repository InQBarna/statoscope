import SwiftUI

struct CounterView: View {
    var body: some View {
        VStack {
            Text("0")
            HStack {
                Button("+") {
                }
                Button("-") {
                }
            }
        }
    }
}
